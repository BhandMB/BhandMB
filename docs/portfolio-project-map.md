# Portfolio Project Map

This repository is the portfolio entry point. Keep project links and positioning aligned with the actual code in each repository.

| Repository | Primary story | Evidence to keep current |
| --- | --- | --- |
| `employee-management-system` | Java/Spring Boot backend with business workflows and role-based access | API setup, security model, tests, screenshots |
| `book-library-api` | REST API design, validation, persistence, and error handling | endpoint examples, database setup, test commands |
| `book-library-app` | Client-side API integration and resilient UI states | environment setup, error handling, screenshots |
| `MiniATM` | Core Java control flow and transaction safety | input rules, transaction invariants, sample runs |
| `LogoGuessApp` | Android Java UI, state management, and local persistence | build instructions, gameplay checks, device notes |

For each project, verify the README contains a working clone command, prerequisites, run instructions, and a concise statement of what was personally implemented.